# Python environment of the coefficient generators (scripts/gen_exp.py, scripts/gen_trig.py).
#   python3 -m venv /tmp/fixed-venv && /tmp/fixed-venv/bin/pip install -r scripts/requirements.txt
# Measured on Python 3.12 (Linux x86-64). gen_exp.py check is green with these versions (and with
# numpy 1.26.4 / 2.5.3, mpmath 1.4.1): its numpy fits are reconciled with the committed
# coefficients (see its docstring). gen_trig.py check is not: its fits have the same platform
# noise and no reconciliation yet.
mpmath==1.3.0
numpy==2.3.3
